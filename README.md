# Repo Manager API

This is the backend of the repo manager project. It is written using Java with Spring Boot framework and Github REST API. Main functionalities include accessing repositories and commits of the authenticated user as well as exporting them in various formats.